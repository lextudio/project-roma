<UserControl
    x:Class="Roma.Host.SearchPane"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="4" />
    </Grid.RowDefinitions>

    <!-- ── Search box + mode combo ── -->
    <Grid Grid.Row="0" Margin="4,4,4,2">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="4" />
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="100" />
      </Grid.ColumnDefinitions>

      <TextBox x:Name="_searchBox"
               Grid.Column="0"
               TextChanged="OnSearchBoxTextChanged"
               KeyDown="OnSearchBoxKeyDown" />

      <TextBlock x:Name="_searchForLabel"
                 Grid.Column="2"
                 Margin="4,0,4,0"
                 VerticalAlignment="Center" />

      <ComboBox x:Name="_modeCombo"
                Grid.Column="3"
                Width="100"
                SelectionChanged="OnModeSelectionChanged">
        <ComboBox.ItemTemplate>
          <DataTemplate>
            <StackPanel Orientation="Horizontal" Spacing="4" VerticalAlignment="Center">
              <Image Width="14" Height="14" Source="{Binding Image}" VerticalAlignment="Center"/>
              <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
            </StackPanel>
          </DataTemplate>
        </ComboBox.ItemTemplate>
      </ComboBox>
    </Grid>

    <!-- ── Results header ── -->
    <Border Grid.Row="1"
            Background="{ThemeResource LayerFillColorDefaultBrush}"
            BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
            BorderThickness="0,1,0,1"
            Height="20">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="20" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="20" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0"
                   Grid.ColumnSpan="2"
                   Text="Name"
                   FontSize="12"
                   FontWeight="SemiBold"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center" />
        <Border Grid.Column="2"
                BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                BorderThickness="1,0,0,0">
          <TextBlock Text="Location"
                     FontSize="12"
                     FontWeight="SemiBold"
                     HorizontalAlignment="Center"
                     VerticalAlignment="Center" />
        </Border>
        <Border Grid.Column="3"
                Grid.ColumnSpan="2"
                BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                BorderThickness="1,0,0,0">
          <TextBlock Text="Assembly"
                     FontSize="12"
                     FontWeight="SemiBold"
                     HorizontalAlignment="Center"
                     VerticalAlignment="Center" />
        </Border>
      </Grid>
    </Border>

    <!-- ── Results list ── -->
    <ListView x:Name="_resultList"
              Grid.Row="2"
              Margin="0"
              SelectionMode="Single"
              DoubleTapped="OnResultDoubleTapped"
              KeyDown="OnResultListKeyDown">
      <ListView.ItemTemplate>
        <DataTemplate>
          <Grid Margin="2,1">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="20" />
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="20" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Member icon -->
            <Image Grid.Column="0"
                   Width="16" Height="16"
                   Source="{Binding Image}"
                   VerticalAlignment="Center" />

            <!-- Name -->
            <TextBlock Grid.Column="1"
                       Text="{Binding Name}"
                       VerticalAlignment="Center"
                       Margin="2,0" />

            <!-- Location icon + text -->
            <StackPanel Grid.Column="2"
                        Orientation="Horizontal"
                        VerticalAlignment="Center"
                        Spacing="2">
              <Image Width="16" Height="16"
                     Source="{Binding LocationImage}"
                     VerticalAlignment="Center" />
              <TextBlock Text="{Binding Location}"
                         Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                         VerticalAlignment="Center" />
            </StackPanel>

            <!-- Assembly icon -->
            <Image Grid.Column="3"
                   Width="16" Height="16"
                   Source="{Binding AssemblyImage}"
                   VerticalAlignment="Center" />

            <!-- Assembly name -->
            <TextBlock Grid.Column="4"
                       Text="{Binding Assembly}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       VerticalAlignment="Center"
                       Margin="2,0" />
          </Grid>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>

    <!-- ── Progress bar ── -->
    <ProgressBar x:Name="_progressBar"
                 Grid.Row="3"
                 IsIndeterminate="False"
                 VerticalAlignment="Stretch" />
  </Grid>
</UserControl>
